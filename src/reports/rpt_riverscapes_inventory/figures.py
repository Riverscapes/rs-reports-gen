import pandas as pd
import plotly.graph_objects as go


def hypsometry_data(df: pd.DataFrame) -> pd.DataFrame:
    """take the hucs dataframe and prepare combined dem"""
    return


def hypsometry_fig(df: pd.DataFrame) -> go.Figure:
    raise NotImplementedError
