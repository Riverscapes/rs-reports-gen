"""DEMO style-guide harness.

Builds a fully-rendered example report that exercises every shared template
pattern (metric cards, highlight cards, figures, tables, page breaks, error
messages) without touching Athena or any real dataset.
"""
