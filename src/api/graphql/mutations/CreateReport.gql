mutation CreateReport($userId: ID, $report: RSReportInput!) {
  createReport(userId: $userId, report: $report) {
    ...ReportDetail
  }
}
fragment ReportDetail on RSReport {
  id
  name

  reportType {
    id
    name
    shortName
    version
  }
  description
  createdAt
  updatedAt

  createdBy {
    id
  }
  status
  progress
}