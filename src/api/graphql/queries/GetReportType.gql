query GetReportType($id: ID!) {
  reportType(id: $id) {
    ...ReportTypeDetail
  }
}
fragment ReportTypeDetail on RSReportType {
  id
  name
  shortName
  subHeader
  description
  version
  ccScript
  # For now parameters can be pretty freeform
  parameters
}